(set-background-color "Black")           ;; 背景
(set-foreground-color "White")           ;; 前景
(set-face-foreground 'region "Darkgray")           ;; 区域前景
(set-face-background 'region "Dimgray")           ;; 区域背景
(setq make-backup-files nil)           ;; 关闭备份
;;(setq backup-directory-alist (quote (("." . "~/.backups"))))          ;; 备份到~/.backups
